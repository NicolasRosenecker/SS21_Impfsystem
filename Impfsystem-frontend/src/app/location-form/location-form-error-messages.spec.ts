import { LocationFormErrorMessages } from './location-form-error-messages';

describe('LocationFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new LocationFormErrorMessages()).toBeTruthy();
  });
});
